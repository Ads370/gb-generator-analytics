version: 2

models:
  - name: stg_mid
    columns:
      - name: price_gbp_per_mwh
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: -200
                max_value: 5000